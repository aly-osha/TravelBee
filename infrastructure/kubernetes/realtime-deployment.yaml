apiVersion: apps/v1
kind: Deployment
metadata:
  name: travelbee-realtime
  namespace: travelbee-prod
  labels:
    app: travelbee-realtime
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: travelbee-realtime
  template:
    metadata:
      labels:
        app: travelbee-realtime
    spec:
      containers:
      - name: realtime
        image: travelbee-realtime:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 5001
        envFrom:
        - configMapRef:
            name: travelbee-config
        env:
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: travelbee-secrets
              key: JWT_SECRET
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: travelbee-secrets
              key: REDIS_URL
        resources:
          limits:
            cpu: "200m"
            memory: "256Mi"
          requests:
            cpu: "50m"
            memory: "128Mi"
        livenessProbe:
          tcpSocket:
            port: 5001
          initialDelaySeconds: 5
          periodSeconds: 10
        readinessProbe:
          tcpSocket:
            port: 5001
          initialDelaySeconds: 5
          periodSeconds: 5
